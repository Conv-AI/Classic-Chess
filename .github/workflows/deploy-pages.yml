name: Deploy GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          lfs: true

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Resolve Vite base path for this Pages site
        shell: bash
        run: |
          BASE_PATH="${{ steps.pages.outputs.base_path }}"
          if [ -z "$BASE_PATH" ] || [ "$BASE_PATH" = "/" ]; then
            VITE_BASE_PATH="/"
          else
            VITE_BASE_PATH="${BASE_PATH%/}/"
          fi
          echo "VITE_BASE_PATH=$VITE_BASE_PATH" >> "$GITHUB_ENV"
          echo "Using Vite base path: $VITE_BASE_PATH"

      - name: Install dependencies
        run: npm ci

      - name: Load frontend environment
        env:
          VITE_CONVAI_API_KEY: ${{ secrets.VITE_CONVAI_API_KEY }}
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.VITE_GOOGLE_CLIENT_ID }}
        shell: bash
        run: |
          if [ -z "${VITE_CONVAI_API_KEY:-}" ]; then
            echo "VITE_CONVAI_API_KEY repository secret is missing or empty."
            echo "Add it under Settings → Secrets and variables → Actions → New repository secret."
            exit 1
          fi
          echo "VITE_CONVAI_API_KEY=$VITE_CONVAI_API_KEY" >> "$GITHUB_ENV"
          if [ -n "${VITE_GOOGLE_CLIENT_ID:-}" ]; then
            echo "VITE_GOOGLE_CLIENT_ID=$VITE_GOOGLE_CLIENT_ID" >> "$GITHUB_ENV"
          fi
          if [ -f .env ]; then
            grep -E '^VITE_' .env | grep -v '^VITE_CONVAI_API_KEY=' | grep -v '^VITE_GOOGLE_CLIENT_ID=' >> "$GITHUB_ENV" || true
          fi

      - name: Build
        run: npm run build -- --base="$VITE_BASE_PATH"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
